class Horario {
    constructor(id, idSala, diaDaSemana, turno, periodo,  idTurma) {
        this.id = id
        this.idSala = idSala,
        this.idTurma = idTurma,
        this.diaDaSemana = diaDaSemana,
        this.turno = turno,
        this.periodo = periodo
    }
}

export default Horario